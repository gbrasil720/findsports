---
name: project-findsports-seo
description: SEO audit and implementation state for findsports.com.br — TanStack Start app, Brazilian football fan/bar matching service
metadata:
  type: project
---

Full SEO audit completed 2026-05-19.

Stack: TanStack Start (SSR) + TanStack Router file-based routing. Meta tags use `head()` export in `createFileRoute` / `createRootRouteWithContext`.

Domain: findsports.com.br

Routes audited:
- `/` — public marketing (homepage, waitlist phase)
- `/login` — auth page
- `/dashboard` — authenticated app
- `/api/**` — API routes (skipped, no meta needed)

State after fixes:
- `__root.tsx`: full OG/Twitter fallback tags, improved title/description, favicon/apple-touch-icon links added
- `index.tsx`: complete OG + Twitter + canonical + `og:locale: pt_BR` + JSON-LD (SoftwareApplication schema)
- `login.tsx`: noindex, nofollow added
- `dashboard.tsx`: noindex, nofollow added
- `public/robots.txt`: Disallow /dashboard and /api/, Sitemap pointer added
- `src/routes/sitemap[.xml].ts`: dynamic sitemap API route created (linter renamed to sitemap.xml)

Outstanding manual assets (cannot be generated by agent):
- `/public/og-image.png` — 1200x630px needed
- `/public/favicon.ico` — generate from logo at favicon.io
- `/public/apple-touch-icon.png` — 180x180px PNG

**Why:** App in waitlist phase targeting Brazilian football fans. SEO matters for organic discovery once launched.
**How to apply:** When adding new public routes, always add full head() with title, description, og:*, twitter:*, canonical, robots: index follow. When adding auth/app routes, only add noindex, nofollow.
